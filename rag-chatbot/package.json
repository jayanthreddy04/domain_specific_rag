{
  "name": "rag-chatbot",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "vercel-build": "npm run build --workspace frontend",
    "build": "npm run vercel-build",
    "dev:frontend": "npm start --workspace frontend",
    "dev:backend": "npm run dev --workspace backend",
    "setup:kb": "npm run setup --workspace backend",
    "evaluate": "npm run evaluate --workspace backend"
  },
  "engines": {
    "node": "20.x"
  }
}
